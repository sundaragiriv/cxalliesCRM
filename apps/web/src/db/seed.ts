// Seed runner. Real seeds (organizations, brands, business_lines, roles) ship in
// P1-03 once the foundation schemas exist. P1-02 ships the runner so `pnpm db:seed`
// is wired and exits 0.

console.log('No seeds yet — P1-03 adds organizations, brands, business_lines, roles.')
