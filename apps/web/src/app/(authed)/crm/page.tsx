import { UsersRound } from 'lucide-react'
import { ModulePlaceholder } from '../_placeholder'

export default function CrmPage() {
  return (
    <ModulePlaceholder
      icon={UsersRound}
      title="CRM"
      ticketRange="P1-14"
      description="Phase 1 ships the contracts + rate cards skeleton needed by Billing. Full deal pipeline lands in Phase 2."
    />
  )
}
