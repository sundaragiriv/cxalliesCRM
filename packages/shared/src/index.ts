// Shared types live here. Phase 1 starts empty; entries land per-ticket as cross-module
// types appear. Keep this file pure types — no runtime code (per architecture §2.2).

export type Brand = 'CXAllies' | 'Pravara.ai'
